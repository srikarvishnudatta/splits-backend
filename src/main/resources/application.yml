spring:
  datasource:
    url: jdbc:postgresql://srikar:postgres@localhost:5432/splits
    driver-class-name: org.postgresql.Driver
    username: srikar
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8080
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
