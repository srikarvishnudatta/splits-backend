spring:
  datasource:
    url: jdbc:postgresql://fj-aws-database.clcuie260lz3.ca-central-1.rds.amazonaws.com/postgres
    driver-class-name: org.postgresql.Driver
    username: srikar_admin
    password: Talkingtom$2001
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8080
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
